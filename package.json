{
  "name": "r7frontendtest",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server --baseDir=public",
    "start": "concurrently \"npm run scss\" \"npm run watch\" \"npm run server\"",
    "compile": "tsc",
    "watch": "tsc -watch",
    "scss": "sass --watch src/stylesheets/:public/stylesheets/ --style=compressed"
  },
  "author": "Kelly Guimarans",
  "license": "ISC",
  "dependencies": {
    "connect": "~3.1.1",
    "serve-static": "~1.5.3"
  },
  "devDependencies": {
    "concurrently": "^6.0.0",
    "lite-server": "^2.6.1",
    "sass": "^1.58.0",
    "typescript": "^4.9.5"
  }
}
